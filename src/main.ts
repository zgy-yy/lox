import { createApp } from 'vue';
import App from './App.vue';

/**
 * todo 语句支持标签，goto语句
 * todo 支持函数重载
 * todo 支持函数默认参数
 * todo 支持函数可变参数
 * todo 支持函数默认参数
 * todo 支持函数默认参数
 * todo 类的外部成员函数定义
 */

// 创建 Vue 应用
const app = createApp(App);
app.mount('#app');
